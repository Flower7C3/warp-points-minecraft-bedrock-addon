name: Test

on:
  pull_request:
    branches: [main, master]
  push:
    branches: [main, master]
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  test:
    uses: Flower7C3/minecraft-addon-workflows/.github/workflows/test.yml@main
